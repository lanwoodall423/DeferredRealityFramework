# Deferred Reality Dev Bridge Workflow

Read the descriptor and run the pure tests before adapter validation. Discover a live bridge and query `lan.deferredreality.framework` context before runtime tests. The typed cooperative adapter may reload after a validated adapter-only build. Gameplay or core changes require a full restart; fresh context is mandatory after reload, restart, or session change.
