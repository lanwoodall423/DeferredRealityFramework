# Deferred Reality Dev Bridge Workflow

Read the descriptor and run the pure tests before adapter validation. Discover a live bridge and query `lan.deferredreality.framework` context before runtime tests. The typed cooperative adapter may reload after a validated adapter-only build. For a coordinator-owned sandbox request `restart request --agent-id <id> --package-id lan.deferredreality.framework --readiness game --save-policy none`; attached/live processes require a person or external orchestrator. Fresh context is mandatory after reload, restart, or session change.
